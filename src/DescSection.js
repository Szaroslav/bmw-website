import React from 'react';
import './DescSection.scss';

const DescSection = ({ contentPos }) => {

    return (
        <section className={'bmw-desc-section content-' + contentPos}>

        </section>
    );
}

export default DescSection;